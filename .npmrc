# Legacy CRA/webpack5 code relies on phantom deps (lodash, history, csstype, ...)
# that were reachable under yarn's flat node_modules. pnpm's default strict,
# non-flat layout breaks those. Hoist everything into node_modules/.pnpm and a
# flat node_modules root, matching yarn's old resolution behavior.
shamefully-hoist=true
